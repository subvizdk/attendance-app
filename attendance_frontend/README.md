# Attendance App (Expo React Native) - Global Levels

## Setup
```bash
cd attendance_app
npm install
npm start
```

## API Base URL
Edit `src/api/client.ts`:
- Android Emulator: `http://10.0.2.2:8000`
- Real phone: `http://YOUR_PC_LAN_IP:8000`
